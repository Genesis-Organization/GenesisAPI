import { Router } from 'express'

import routesShared from './api/shared/index'
// import cors from '@/middlewares/cors'

const router: Router = Router()

// router.use(cors)
// router.use('/shared', routesShared)

export default router
